{
	"name": "tinawiki",
	"version": "1.0.0",
	"scripts": {
		"build": "echo \"Building static\" && cp -R *",
		"deploy": "wrangler publish"
	},
	"devDependencies": {
		"@cloudflare/wrangler": "^3.0.0"
	}
}